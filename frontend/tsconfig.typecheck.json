{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "skipLibCheck": true
  },
  "include": ["app", "components", "modules"],
  "exclude": [
    "tests",
    "**/__tests__/**",
    "**/*.test.*",
    "**/*.spec.*",
    "**/*.stories.*",
    "playwright.*",
    "vitest.*",
    "jest.*"
  ]
}
